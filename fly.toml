app = "tgbot"
primary_region = "waw"  # Warsaw — close to Ukraine

[build]

[env]
  PYTHONUNBUFFERED = "1"

[[services]]
  internal_port = 8080
  protocol = "tcp"
  [services.concurrency]
    hard_limit = 10
    soft_limit = 5

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
